[package]
name = "dirtydata-host"
description = "Plugin Sandbox / Foreign Boundary — stub"
version.workspace = true
edition.workspace = true

[dependencies]
bytemuck = "1.14"
thiserror = "1.0"
dirtydata-core = { path = "../dirtydata-core" }
libloading = "0.9.0"
vst3 = "0.3.0"
