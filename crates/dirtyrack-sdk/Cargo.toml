[package]
name = "dirtyrack-sdk"
version = "0.1.0"
edition = "2021"
description = "Software Development Kit for DirtyRack deterministic modules."

[dependencies]
wide = "0.7"
libm = "0.2"
serde = { version = "1.0", features = ["derive"] }
